{
  "name": "free-invoice-generator",
  "version": "1.0.0",
  "description": "Free invoice generator with PDF export and modern web UI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ]
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^24.43.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.0",
    "supertest": "^7.2.2"
  }
}
